apiVersion: v1
kind: Service
metadata:
  name: service-api
  labels:
    app.kubernetes.io/name: service-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: service-api
  ports:
    - name: http
      port: 80
      targetPort: http

